import { StyleSheet } from 'react-native';

import { colors } from 'consts';

const styles = StyleSheet.create({
  border: {
    width: '100%',
    borderBottomColor: colors.neutrals['500'],
    borderBottomWidth: 0.75,
  },
});

export default styles;
